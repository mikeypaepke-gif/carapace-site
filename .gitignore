.DS_Store
.wrangler/
node_modules/
*.log
.dev.vars

# DMGs are served via Cloudflare Pages uploads; don't bloat git with binaries
Carapace-*.dmg

# Claude Code session metadata (per-machine, not source)
.claude/
